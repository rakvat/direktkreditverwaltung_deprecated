<h1>Listing contacts</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Vorname</th>
    <th>Adresse</th>
    <th>E-Mail</th>
    <th>Telefon</th>
    <th>Kontonummer</th>
    <th>BLZ</th>
    <th>Bankname</th>
    <th>Bemerkung</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contacts.each do |contact| %>
  <tr>
    <td><%= contact.name %></td>
    <td><%= contact.prename %></td>
    <td><%= contact.address %></td>
    <td><%= contact.email %></td>
    <td><%= contact.phone %></td>
    <td><%= contact.account_number %></td>
    <td><%= contact.bank_number %></td>
    <td><%= contact.bank_name %></td>
    <td><%= contact.remark %></td>
    <td><%= link_to 'Anzeigen', contact %></td>
    <td><%= link_to 'Editieren', edit_contact_path(contact) %></td>
    <td><%= link_to 'Löschen', contact, method: :delete, data: { confirm: 'Sicher?' } %></td>
    <td><%= link_to 'Vertrag erstellen', new_contact_contract_path(contact) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Neuen Kontakt erstellen', new_contact_path %>
